import { AppColors } from "../../utils/AppColors"

export const Menu = () => {
    return (
        <div className="container" style={{ backgroundColor: AppColors.primaryColor }}>
            <button>Text</button>
        </div>
    )
}